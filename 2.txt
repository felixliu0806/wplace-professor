 1. Overlay 的触发: "点击按钮代表用户已经调整好图片，后续用户鼠标点击到哪里..." 这里的“点击按钮”是指 Popup
      里的哪个按钮？是“转换”之后的“确认/放置”按钮吗？还是指在 overlay 出现后，有一个确认中心点的按钮？
      确认/放置按钮，点击确认之后，放置overlay不需要再次确认
   2. Overlay 的交互细节:
       * overlay 的像素画是静态图片还是可以交互的（比如点击色块直接绘制到画布）？根据描述，它更像是一个参考图层。
	是的，参考图层，先不做交互，后面迭代可能会做交互
       * “高亮所有图片中的对应颜色。其他色块均变暗。” 这个高亮/变暗的效果具体如何实现？是改变透明度、亮度，还是加一个边框？
	改变透明度及亮度
   3. 用户手动选择与自动检测的优先级:
       * 如果用户手动勾选了一个自动检测为未解锁的颜色，后续在页面上使用这个颜色时会发生什么？是提示用户无法使用，还是允许用户尝试（可能会
         失败）？
	我们模拟点击那个颜色的时候，网页本身会弹窗告知无法使用这个颜色的
   4. 放置位置的精确性:
       * 以点击位置为“中心点”放置 overlay，这个“中心点”是指 overlay 容器的中心与点击点对齐，还是 overlay
         图像本身的中心与点击点对齐？这可能影响最终位置的计算。
	overlay图像本身，侧边栏只是辅助
   5. 错误处理和边界情况:
       * 如果上传的图片过大或格式不支持，如何提示用户？
	弹窗提示即可
       * 如果 pixelit 转换过程出错怎么办？
	弹窗提示错误
       * 如果在非 wplace 页面使用插件，或者页面结构发生变化导致无法检测颜色，插件如何表现？
	只在wplace.live页面可以使用插件


UI问题
pop窗口还需要重新设计一下，我希望是侧边栏弹出一栏。上传部分上传完成之后，展示原图的缩略图。下面紧接着就是转换好的图片预览区。最下面是调色盘。最下面是放置按钮。

另外补充一些资料：
1. pixel.md 中包含如何使用pixelit这个开源项目的介绍，其依赖js，不知道放到哪，你看看放到一个合适的位置。我先放到根目录了pixelit.min.js。

最后总体再看看是否还有有歧义的地方